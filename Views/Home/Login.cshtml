@model FMS.Models.fms_users;
@{
    ViewData["Title"] = "Home Page";
    Layout = "/Views/Shared/_Layout.cshtml";
}



<style>
    .btn-color {
        background-color: #0e1c36;
        color: #fff;
    }

    .profile-image-pic {
        height: 150px;
        width: 150px;
        object-fit: cover;
    }



    .cardbody-color {
        background-color: #ebf2fa;
    }

    a {
        text-decoration: none;
    }
</style>



<div class="container">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card my-5">
                <form asp-controller="Home" asp-action="Login" method="post" class="card-body cardbody-color p-lg-5">
                    @if (ViewBag.status == 0)
                    {
                        <div class="alert alert-danger">@ViewBag.msg</div>

                    }
                    <div class="text-center">
                        <img src="~/images/fms.png" class="img-fluid profile-image-pic img-thumbnail rounded-circle my-3" width="200px" alt="profile">
                    </div>

                    <div class="mb-3">
                        @Html.TextBoxFor(m => m.Username, new {@class = "form-control", @placeholder = "Username"})

                    </div>
                    <div class="mb-3">
                        @Html.PasswordFor(m => m.Password, new {@class = "form-control", @placeholder = "Password"} )

                    </div>
                    <div class="text-center">
                        <button type="submit" class="btn btn-color px-5 mb-5 w-100" onclick="preventBack()">Login</button>
                    </div>
                    <div id="emailHelp" class="form-text text-center mb-5 text-dark">
                        Not Registered? <a href="#" class="text-dark fw-bold"> Create an Account
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>



@*<form id ="login_form" class="form-outline mb-4" asp-controller="Home" asp-action="Index" method="post">
        <table class="table table-responsive table-bordered">
            <thead>
            @if (ViewBag.status == 0)
            {
                <div class="alert alert-danger">@ViewBag.msg</div>
            }
                <tr class="row-cols-1">
                <th scope="col" colspan="2" style="min-height : 140px !important;">Welcome to FMS</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                <td style="text-align:left;">
                    <label class="form-label">User Name :</label>
                    @Html.TextBoxFor(m => m.Username, new {@class = "form-control", @placeholder = "your user name"})

                    </td>
                </tr>
                <tr>
                    <td style="text-align:left;">
                    <label class="form-label">Password :</label>
                    @Html.PasswordFor(m => m.Password, new {@class = "form-control", @placeholder = "your password"} )
                    </td>
                </tr>
            </tbody>

            <tfoot>
                <tr>
                    <td colspan="2" style="text-align:center;">
                    <input type="submit" asp-action="" asp-controller="Home" class="btn btn-primary"  style="text-align:center; width:180px;"/>
                    <input type="reset" name="Reset" class="btn btn-primary" asp-action="" asp-controller="" style="width:180px;" onclick="reset()" />
                    </td>
                </tr>
            </tfoot>

        </table>
    </form>*@






